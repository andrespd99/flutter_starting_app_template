// ignore_for_file: prefer_const_constructors

import 'package:flutter_test/flutter_test.dart';
import 'package:my_utils/my_utils.dart';

void main() {
  group('MyUtils', () {
    test('can be instantiated', () {
      expect(MyUtils(), isNotNull);
    });
  });
}
